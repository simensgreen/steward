# Instance Product catalog with per-Store prices

Products are instance-level (shared barcode/OFF catalog). The same Product can have different Store Prices and promotions per Store. Households and ledgers reference Products; they do not clone the catalog per Household by default.
