# Trust Level: Person default + per-Fund override

Each Person has a global default Trust Level (confirm receipt vs auto-accept on incoming Money Requests) and may set a different Trust Level for each Fund. Fund-specific override wins when acting in that Fund.
