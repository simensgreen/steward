# Stock consumption: conversion, reject shortfall, FEFO/FIFO

Each Product has an exact Purchase↔Consumption conversion and precision. Consumption rejects insufficient quantity (no negative stock by default). Consume FEFO then FIFO; ties break on a stable Stock Entry id.
