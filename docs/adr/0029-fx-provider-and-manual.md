# FX provider + manual rates

Exchange rates come from a pluggable provider (e.g. ECB) with manual override. Transaction FX multipliers freeze at post time against the ledger Default Currency. Product dual-currency display uses the current rate.
