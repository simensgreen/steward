# Catalog edits are instance-wide

Manual Product/Recipe edits apply to the instance catalog (with revision/source metadata, e.g. OFF vs human). Not per-Person overlays or per-Household private clones by default.
