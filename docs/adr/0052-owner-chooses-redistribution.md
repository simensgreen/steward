# Owner force-remove: onto self or Fund rules

On Owner force-remove of a Fund Participant with open obligations, the Owner chooses exactly one of: absorb everything onto themselves, or apply the Fund’s configured redistribution rules. No silent drop, and no free-form ad-hoc split invented only at remove time.
