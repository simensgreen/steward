# Self-host first, hosted later

Steward is designed and shipped primarily as a self-hosted install (one or more Households per instance). A hosted offering by the author is an optional later path, not the v1 product bet. Auth, data locality, backups, and push must work well without a central Steward cloud; any hosted mode must not force cloud-only assumptions into the core model.
